using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RequiredMatsText : MonoBehaviour
{
    public ShopInventory Armour;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
     //  Armour.cost
       // ScriptableObject
    }
}
